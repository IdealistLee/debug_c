#include <stdio.h>
/*这是一个转换ASCII为字符的程序*/
int main(void)
{
    int f;

    printf("please input a numer:");
    scanf("%d",&f);
    printf("The value is %c",&f);

    getchar();
    getchar();

    return 0;
}

printf("The value is %c",&f);  >> printf("The value is %c",f)
注:&f是取用地址，f是打印变量

